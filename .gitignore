# Personal, machine-specific files — imported by CLAUDE.md but never shared.
.local/

# Rust build output.
/target/

# The built documentation site. The pages are the source; this is what the
# builder makes of them.
/book/

# Mutation testing scratch. cargo-mutants copies the tree here to change it.
/mutants.out*/
