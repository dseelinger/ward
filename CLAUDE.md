# ward

## Project

<!-- Shared, checked-in instructions for everyone working on this repo. -->

## Local overrides

The import below pulls in personal, machine-specific instructions from `.local/CLAUDE.md`.
That path is gitignored and optional — if it does not exist, Claude Code silently skips it and
the rest of this file still loads. Do not wrap the import in backticks or a code fence; imports
inside code spans are ignored by the loader.

@./.local/CLAUDE.md
